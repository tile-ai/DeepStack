# TileSight model components

This directory contains the TileSight components used by DeepStack's operator,
cache and distributed-performance models. Architecture-specific DeepStack
presets live under `src/deepstack/mosaic/arch/`.

DeepStack uses its own NoC and parallelism modules under `mosaic/`.
